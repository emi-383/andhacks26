<section id="register" class="regis-section">
  <div class="regis-shell">
    <h2 class="regis-heading">Join &amp;Hacks</h2>

    <div class="regis-grid">

      <div class="regis-card">
        <span class="regis-label">Hacker</span>
        <p>Register now to secure your spot. Open to all majors — no prior experience in coding needed!</p>
        <a href="https://wmsas.qualtrics.com/jfe/form/SV_6S8vNLg8wb4xpum" target="_blank" class="regis-btn">Register</a>
      </div>

      <div class="regis-card">
        <span class="regis-label">Sponsor</span>
        <p>Connect with innovative talent, showcase your brand, and support budding potential! Find details and prospectus linked below.</p>
        <a href="https://wmsas.qualtrics.com/jfe/form/SV_57iVqHk2QHKUzpY" target="_blank" class="regis-btn">Sponsor</a>
      </div>

    </div>
  </div>
</section>

<style>
  .regis-section {
    padding: 2rem 2rem 6rem;
    scroll-margin-top: 80px;
  }

  .regis-shell {
    max-width: 1100px;
    margin: 0 auto;
  }

  .regis-heading {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    text-align: center;
    line-height: 1;
    margin: 0 0 2rem;
  }

  .regis-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .regis-card {
    border: 1px solid currentColor;
    border-radius: 1.4rem;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    text-align: center;
    align-items: center;
    background:
      radial-gradient(circle at top left, rgba(255, 255, 255, 0.12), transparent 40%),
      linear-gradient(135deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.02));
    backdrop-filter: blur(12px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
  }

  .regis-label {
    font-size: clamp(1.4rem, 3vw, 2rem);
    font-weight: 700;
  }

  .regis-card p {
    margin: 0;
    line-height: 1.7;
    opacity: 0.88;
    flex: 1;   /* pushes button to bottom */
  }

  .regis-btn {
    display: inline-block;
    padding: 0.8rem 2rem;
    border-radius: 50px;
    border: 1px solid currentColor;
    color: inherit;
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    backdrop-filter: blur(20px) saturate(180%); 
    background: rgba(255, 255, 255, 0.05);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.3);
    transition: background 0.2s, transform 0.2s;
    align-self: center;
  }

  .regis-btn:hover {
    transform: scale(1.05);
    background: rgba(255, 255, 255, 0.06);
    box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.4),
          0 4px 32px rgba(0, 0, 0, 0.17);
    text-shadow:
          0 0 8px rgba(255, 255, 255, 0.9),
          0 0 20px rgba(255, 255, 255, 0.5);
  }

  @media (max-width: 640px) {
    .regis-grid {
      grid-template-columns: 1fr;  /* stack on mobile */
    }

    .regis-section {
      padding-inline: 1rem;
    }
  }
</style>